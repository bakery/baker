import { DEFAULT_ACTION } from '../action-types';

function {{reducerName}}(state = {}, action) {
  switch (action.type) {
    case DEFAULT_ACTION:
      return state;
    default:
      return state;
  }
}

export default {{reducerName}};